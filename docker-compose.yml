services:
  dance-challenge:
    build: .
    container_name: dance-partner-challenge
    ports:
      - "8443:8443"
    volumes:
      - ./data:/app/data
      - ./generated-qr:/app/generated-qr
    environment:
      - SPRING_DATASOURCE_URL=jdbc:sqlite:/app/data/dance.db
      - APP_QR_OUTPUT_DIR=/app/generated-qr
    restart: unless-stopped
